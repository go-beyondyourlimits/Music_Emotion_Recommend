version: '3'
services:
  app:
    container_name: spotify_app
    image: spotify_app
    build: ./
    ports:
      - 8888:8888
      - 3000:3000
    tty: true
    stdin_open: true
    volumes:
      - ./spotify-music-search-app:/root/spotify-music-search-app
    restart: always
